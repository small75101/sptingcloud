spring:
  application:
    name: eurekaclientbusamqp
  cloud:
    config:
      name: config-client
      label: master
      #uri: http://localhost:8080/
      profile: dev
      discovery:
        enabled: true
        service-id: eurekaserverconfig
eureka:
  client:
    service-url:
      defaultZone: http://localhost:2000/eureka/
